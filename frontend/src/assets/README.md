# Assets Directory

## Logo
Save the KhushiBites logo image here as `logo.png`.

The logo is referenced in the app components. Once saved, you can use it with:
```javascript
import logo from '../../assets/logo.png';
// <Image source={logo} style={{ width: 44, height: 44 }} />
```
